meta {
  name: up
  type: http
  seq: 3
}

post {
  url: {{host}}/control/navigate
  body: json
  auth: inherit
}

body:json {
  {
    "direction": "up"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
