meta {
  name: search
  type: http
  seq: 13
}

post {
  url: {{host}}/control/search
  body: json
  auth: inherit
}

body:json {
  {
    "query": "The office"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
