meta {
  name: left
  type: http
  seq: 5
}

post {
  url: {{host}}/control/navigate
  body: json
  auth: inherit
}

body:json {
  {
    "direction": "left"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
